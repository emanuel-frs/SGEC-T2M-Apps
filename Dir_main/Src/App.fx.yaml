App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =Set(Height, 0);
        Set(Width, 0);
        Set(FontSize, 14);
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

