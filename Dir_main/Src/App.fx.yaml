App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =Set(update, 0);
        Set(FontSize, 16);
        Set(DarkMode, false);
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

